- To run the build and get a local server run the command in
./preview.sh


# Extensions 
- shiny (for python) - after

<!-- temp notes 
# This creates a "shortcut" so tools think Podman is Docker
alias docker=podman

How to make them "speak" Podman:
In your VS Code settings.json, you must point both to the Podman executable:
{
    "dev.containers.dockerPath": "podman",
    "docker.dockerPath": "podman",
    "docker.host": "unix:///run/user/1000/podman/podman.sock" 
}

-->