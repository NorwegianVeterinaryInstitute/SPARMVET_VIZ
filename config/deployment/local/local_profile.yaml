# SPARMVET Local Development Profile (ADR-048)
# Profile Resolution Level 4 — dev repo fallback.
# Used when SPARMVET_PROFILE env var is unset and no user/system profile exists.
deployment_type: filesystem
deployment_name: "SPARMVET_VIZ (Local Dev)"
default_persona: "developer"

locations:
  # Location 1: Raw ingestion data
  raw_data: "assets/test_data/"
  
  # Location 2: Pipeline Manifest definitions
  manifests: "config/manifests/pipelines/"
  
  # Location 3: Curated Anchors & Views (Tiers 1 & 2 Parquet)
  curated_data: "tmp/UI_TEST/parquet_data/"
  
  # Location 4: User specific artifacts, sessions, and Tier 3 outputs
  user_sessions: "tmp/UI_TEST/user/"
  
  # Location 5: Gallery examples and recipes
  gallery: "assets/gallery_data/"

scripts:
  test_data_gen: "libs/generator_utils/src/generator_utils/aqua_synthesizer.py"
  excel_parser: "libs/ingestion/src/ingestion/excel_handler.py"

runtime:
  python_interpreter: "./.venv/bin/python"
