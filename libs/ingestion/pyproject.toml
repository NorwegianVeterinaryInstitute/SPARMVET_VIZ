[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ingestion"
version = "0.1.0"
dependencies = ["polars>=1.0.0"]

[tool.setuptools]
package-dir = {"ingestion" = "src"}
packages = ["ingestion"]
