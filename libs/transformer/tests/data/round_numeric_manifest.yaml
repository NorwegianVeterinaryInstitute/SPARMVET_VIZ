data_schemas:
  test_round:
    input_fields:
      sample_id: { is_primary_key: true }
      val_to_round: {}
    wrangling:
      - action: "round_numeric"
        columns: ["val_to_round"]
        decimals: 2
    output_fields:
      sample_id: { is_primary_key: true }
      val_to_round: {}
