[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "utils"
version = "0.1.0"
description = "SPARMVET_VIZ utils library"
dependencies = [
    "pyyaml>=6.0.0",
    "polars"
]

[tool.setuptools.packages.find]
where = ["src"]
