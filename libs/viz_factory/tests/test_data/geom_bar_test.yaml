data_path: "./geom_bar_test.tsv"
plots:
  bar_plot:
    mapping:
      x: "region"
      fill: "region"
    layers:
      - name: "geom_bar"
        params:
          show_legend: false
      - name: "theme_minimal"
        params:
          base_size: 11
