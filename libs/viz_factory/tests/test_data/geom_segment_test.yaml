id: geom_segment_test
data_path: ./geom_point_test.tsv
wrangling:
  tier2:
  - action: mutate
    column: xend
    expression: pl.col('x') + 1
  - action: mutate
    column: yend
    expression: pl.col('y') + 1
plots:
  main:
    mapping:
      x: x
      y: y
      xend: xend
      yend: yend
    layers:
    - name: geom_segment
      params: {}
