id: "theme_publication_test"
description: "Test manifest for theme_publication_test.yaml"
input_fields: []
wrangling: []
output_fields: []
data_path: "./theme_test_data.tsv"
plots:
  pub_plot:
    mapping:
      x: "x"
      y: "y"
      color: "group"
    layers:
      - name: "geom_point"
        params:
          size: 5
      - name: "theme_publication"
        params:
          base_size: 10
      - name: "theme_minimal" # Combine to see if it works? No, one by one.
